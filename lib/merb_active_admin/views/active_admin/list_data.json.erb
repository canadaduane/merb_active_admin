<%=
  { "page"  => (@paginated.current_page rescue 0),
    "total" => (@paginated.pagination_record_count rescue 0),
    "rows"  =>
      @objects.map{ |o|
        { "id"   => o.id,
          "cell" => @model.columns.map{ |c| o.send( c ) }
        }
      }
  }.to_json
%>